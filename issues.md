ALL THESE ISSUES ARE RELATING TO THE SMALLER-SCREEN RESPONSIVENESS.

1. In the Task page @TaskCOntentPage change all the sm: to md:
2. Still in the task page some content are too long in the x-direction and are cuasing the page to float to the right.. find a way to make it display properly.
3. in the dashboard the hud box are too big am talking about the hud for the total note, total due today.
4. In the Ideas page let the upload image, voice icon and the capture let them be represented by icons only in smaller screen
5. In the note detailed page the header are too big for smaller screen because they are fixed or sticky to the and when the user is scrolling on smaller devices 60% of the screen is already covered by the sticky componenet. so find something to do about it maybe the moment the user get to the part of the note the note fullscren will be activated and when the user get to the bottom of the note the fullscren should be deactivated .
6. In the tag page when the user click a single tag the color list display are separated into row when all the color can stay in a single row.
7. The dreams/:id that is the dream detailed page when the user click a particular dream is giving a 404 not found error and also check whether it is exported safely i te capacitor apk
8. In the ledger page , the hud logged completion and the focused minutes can be make into 2 per rows and very small .
9. In the profile page, 1. The header is not looking perfect in the smaller screen. 2. the component that is next to the after the header make the listing 2 per row and very small . 3. also the execution profile also of make it 2 per row and very small .
10.. In the notification page at the header and the alert 1. check whether asking the user for notificationnthe ui is set and also when i add a task and i click the link of the task it give 404 error and is because the way the single task page render is different check and fixed.. 


ALL THE UI/UX ISSUES ARE PARTICULARLY FOR SMALLER SCREEN. 