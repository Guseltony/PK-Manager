import CalendarPage from "@/src/features/calendar/CalendarPage";

export const metadata = {
  title: "Calendar | PK-Manager",
  description: "Coordinate tasks, focus sessions, journals, and productivity history on one timeline.",
};

export default function CalendarRoutePage() {
  return <CalendarPage />;
}
