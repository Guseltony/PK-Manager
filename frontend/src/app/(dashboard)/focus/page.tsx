import FocusModePage from "@/src/features/focus/FocusModePage";

export const metadata = {
  title: "Focus Mode | PK-Manager",
  description: "A decision engine for the most important tasks to execute right now.",
};

export default function FocusPage() {
  return <FocusModePage />;
}
