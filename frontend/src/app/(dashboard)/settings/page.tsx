import SettingsPage from "@/src/features/settings/SettingsPage";

export const metadata = {
  title: "Settings | PK-Manager",
  description: "Control the behavior of your PKM intelligence, automation, and accountability systems.",
};

export default function SettingsRoutePage() {
  return <SettingsPage />;
}
