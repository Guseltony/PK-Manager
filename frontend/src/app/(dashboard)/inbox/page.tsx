import InboxPage from "@/src/features/inbox/InboxPage";

export const metadata = {
  title: "Inbox | PK-Manager",
  description: "Capture raw thoughts and route them automatically across the PKM system.",
};

export default function InboxRoutePage() {
  return <InboxPage />;
}

